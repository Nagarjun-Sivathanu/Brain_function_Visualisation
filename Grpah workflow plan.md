Now lets work on a new branch called Hirarchy based strucutreing : 
1) Make this a seprate branch in git hub 
2) Lets use the oontology tree that we came up with in the name cleaned_brain_anatomy.json for the hirachy and the strcutres we have present and acesse to then for the connections it may have otehr than the three ontology and rather the cross one look at atlasStructure_filtered.json to see if it has connections to other regions in the brain  
3) Have this seprate internaly / within the main file seprately so that i can demo both of it at the same time if needed so dont over write any of the current existing and do this . Just have this seprate and not affect the brod cast model is what i am trying to say  
4) The idea is that when a quries is pased the agent which recives it it will always be first brain which decides who under it should get it and so on then we go to it then from there .
5) As ussal let the final output and the hierarchy be debated on 
6) Then like the current let it give out the final report as json and the outputs as the current one and the route of json it has gone threw to give the output 